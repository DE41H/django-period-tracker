[project]
name = "period-tracker"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "django>=6.0.4",
]

[dependency-groups]
dev = [
    "django-stubs>=6.0.2",
    "ruff>=0.15.9",
]

[tool.ruff]
select = ["E", "F", "DJ"]
